# Database (optional - will use SQLite if not provided)
DATABASE_URL=postgresql://user:password@host:port/database

# OpenAI API Key (optional - will use fallback assessments if not provided)
OPENAI_API_KEY=sk-...

# Port (default: 8000)
PORT=8000
