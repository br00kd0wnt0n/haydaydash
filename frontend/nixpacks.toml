[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["rm -rf node_modules/.vite node_modules/.cache dist && npm run build"]

[start]
cmd = "npx serve -s dist -l $PORT"
