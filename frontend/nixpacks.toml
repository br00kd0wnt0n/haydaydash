[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
# Ensure fresh build every deploy
cmds = ["echo 'Building v2.1.0...' && rm -rf dist && npm run build"]

[start]
cmd = "npx serve -s dist -l $PORT"
